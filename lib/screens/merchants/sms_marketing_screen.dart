import 'package:flutter/material.dart';

class SMSMarketingScreen extends StatelessWidget {
  const SMSMarketingScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(  
        title: Text("SMS Marketing"),
      ),
      body: ListView(  
        children: [
          //
        ],
      ),
    );
  }
}