onItemDetailPageStartView({String deviceId, String timeStamp, String itemId}) async {
  // Call http backend
}

onItemDetailPageEndView({String deviceId, String timeStamp, String itemId}) async {
  // Call http backend
}