import 'package:flutter/material.dart';

class ServicesCategoryViewAll extends StatelessWidget {
  const ServicesCategoryViewAll({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar()
    );
  }
}